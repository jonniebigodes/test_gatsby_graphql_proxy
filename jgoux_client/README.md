<!-- AUTO-GENERATED-CONTENT:START (STARTER) -->
# GRAPHQL Configuration

Using Apollo boost to streamline the client implementation.
Check `gatsby-browser.js` and `gatby-ssr.js` for the implementation.

`gatsby.config.js` has the proxy config.
